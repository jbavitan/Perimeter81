const goalsEndpoint = '/goals';
const stepsEndpoint = '/steps';
const serverUrl = 'http://localhost:4000'

const updateEndpoint = '/update';
const createEndpoint = '/add';
const completeEndpoint = '/complete';

export {
    goalsEndpoint,
    stepsEndpoint,
    updateEndpoint,
    createEndpoint,
    completeEndpoint,
    serverUrl
}